---
interface Props {
  class?: string;
  tag?: 'div' | 'section' | 'header' | 'footer' | 'nav';
}

const { class: className = '', tag: Tag = 'div' } = Astro.props;
---

<Tag class={`mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 ${className}`}>
  <slot />
</Tag>
